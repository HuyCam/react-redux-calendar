import React, { Component } from 'react';

class AddReminder extends Component {
    render() {
        return (
            <div>
                Add a new reminder here
            </div>
        );
    }
}

export default AddReminder;